import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  titulo: {
    fontSize: 20,
    fontWeight: "bold",
    marginBottom: 10,
  },
  texto: {
    fontSize: 16,
  },
  textoBy: {
    fontSize: 14,
    fontWeight: "bold",
  },
  espaco: {
    marginBottom: 10,
  },
});

export default styles;
